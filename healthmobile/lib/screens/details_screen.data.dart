import 'package:flutter/material.dart';

class DetailsScreenData extends StatelessWidget {
  const DetailsScreenData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
